const keyMap = {
  ArrowLeft: "left",
  ArrowDown: "down",
  ArrowRight: "right",
  ArrowUp: "up",
};

const opositeDirectionMap = {
  left: "right",
  right: "left",
  up: "down",
  down: "up",
};

const gridSize = 10;

export { gridSize, keyMap, opositeDirectionMap };
